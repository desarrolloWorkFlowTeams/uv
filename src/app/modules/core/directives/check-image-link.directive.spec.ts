import { CheckImageLinkDirective } from './check-image-link.directive';

describe('CheckImageLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckImageLinkDirective();
    expect(directive).toBeTruthy();
  });
});
